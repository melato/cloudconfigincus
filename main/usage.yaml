short: applies cloud-init configuration to Incus instances
long: |
  configures instances (containers, VMs), by
  applying cloud-init config files, using the Incus API.
use: "[options] args"
examples:
- apply -i <instance> <config-file.yaml>
commands:
  version:
    short: print version
  apply:
    use: "[-i <instance>] <config-file.yaml>..."
    short: apply cloud-init files to an instance

